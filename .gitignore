# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Build artifacts
*.zip

# WoW SavedVariables — never commit these, they contain per-character data
*.lua.bak

# Local-only reference material that lives in the parent folder
# (F:\Claude\Addons\). Never commit copies of these even if someone
# accidentally drags them into the repo directory.
addon-conventions.md
lua-reference/
wow-api/
AutoDelete_Architecture.md
Addon_UI_StyleGuide.md
CLAUDE.md
handoff/